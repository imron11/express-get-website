{
  "name": "express-get-website",
  "scripts": {
    "clean": "rm -rf dist",
    "start": "nodemon index.ts",
    "start:prod": "yarn clean && yarn build && node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  },
  "dependencies": {
    "@types/node": "^20.10.1",
    "axios": "^1.6.2",
    "express": "^4.18.2",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  }
}
